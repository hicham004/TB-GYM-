<Project Sdk="Microsoft.NET.Sdk.Worker">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <UserSecretsId>tb-gym-worker</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- The Worker SDK's own generic-host dependency. Pinned centrally to the repository's .NET 10
         patch version like every other Microsoft.Extensions package; it is not a new third-party
         dependency, and nothing else was added for this process. -->
    <PackageReference Include="Microsoft.Extensions.Hosting" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\TB.Gym.Infrastructure\TB.Gym.Infrastructure.csproj" />
    <ProjectReference Include="..\Modules\Notifications\TB.Gym.Modules.Notifications\TB.Gym.Modules.Notifications.csproj" />
  </ItemGroup>

</Project>
