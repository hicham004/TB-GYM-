{
  "name": "DayNote",
  "type": "object",
  "properties": {
    "program_id": {
      "type": "string",
      "description": "Reference to TrainingProgram"
    },
    "client_id": {
      "type": "string",
      "description": "Reference to User (client)"
    },
    "week": {
      "type": "number"
    },
    "day": {
      "type": "number"
    },
    "coach_note": {
      "type": "string",
      "description": "Note written by coach — visible to both coach and client"
    },
    "client_note": {
      "type": "string",
      "description": "Note written by client — visible to both coach and client"
    }
  },
  "required": [
    "program_id",
    "client_id",
    "week",
    "day"
  ]
}
